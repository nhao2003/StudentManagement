<UserControl x:Class="StudentManagement.Component.ClassListDetails.ClassDetails.SubjectTeacher"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudentManagement.Component.ClassListDetails.ClassDetails"
             mc:Ignorable="d" 
             d:DesignHeight="42" d:DesignWidth="350">
    <Border Background="White" Width="310" Height="42">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Border Background="LightGray" Grid.Row="0"/>
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Foreground="Gray" Text="{Binding Subject}" Margin="20,0,0,0"/>
                <TextBlock Text="{Binding Teacher}" HorizontalAlignment="Right" FontWeight="DemiBold" Margin="0,0,20,0"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
